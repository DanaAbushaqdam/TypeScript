# User Management System – TypeScript

##  Overview

This project implements a simple **User Management System**. 
The system allows for managing user data through operations such as adding, updating, removing, and retrieving users. 
